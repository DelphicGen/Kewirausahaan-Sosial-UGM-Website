<%- include('../partials/header') %>
<%- include('function') %>

<div class="form d-flex flex-column justify-content-center mt-5">

    <h1>Add <%= table[0].toUpperCase() %><%= table.slice(1).replace(/_([a-z])/g, function(g) { return g[1].toUpperCase() }) %>'s Table</h1>
    
    <% if (table == "users") { %>
        <form action="/register" method="POST">
    <% } else { %>
        <form action="/new?table=<%= table %>" method="POST">
    <% } %>
        <% columns.forEach((column) => { %>
            <% if(column.Field !== 'id' && column.Field !== 'role' && column.Field !== 'created' && column.Field !== 'resetPasswordToken' && column.Field !== 'resetPasswordExpires' ) { %>
                <div class="form-group">
                    <label for="<%= column.Field %>">
                        <%= column.Field %>
                        <% if(column.Null == 'NO') { %>
                            *
                        <% } %>
                    </label>
                    
                    <% if(column.Null == 'NO') { %>
                        <% if (column.Field === 'password') { %>
                            <input type="password" class="form-control" id="<%= column.Field %>" name="<%= column.Field %>" required>
                        <% } else if (column.Field === 'date') { %>
                            <input type="datetime-local" class="form-control" id="<%= column.Field %>" name="<%= column.Field %>" required>
                        <% } else if (column.Field === 'image') { %>
                            <input type="file" class="upload form-control" id="<%= column.Field %>" name="<%= column.Field %>" required>
                            <div class="d-flex flex-sm-row align-items-lg-center">
                                <div class="box-2">
                                    <div class="result"></div>
                                </div>
                                <div class="box-2 img-result">
                                    <img class="cropped" src="" alt="">
                                </div>
                            </div>
                            <button class="btn save d-none">Save</button>
                            <a href="" class="btn download d-none">Download</a>
                        <% } else if (columns.Field === 'full_details') { %>
                            <%= columns.Field %>
                            <textarea class="form-control ckeditor" id="<%= columns.Field %> ckeditor" name="<%= columns.Field %>" required><%= value %></textarea>
                        <% } else { %>
                            <input type="text" class="form-control" id="<%= column.Field %>" name="<%= column.Field %>" required>
                        <% } %>
                    <% } else { %>
                        <% if (column.Field === 'image') { %>
                            <input type="file" class="form-control" id="<%= column.Field %>" name="<%= column.Field %>">
                            <div class="d-flex flex-sm-row align-items-lg-center">
                                <div class="box-2">
                                    <div class="result"></div>
                                </div>
                                <div class="box-2 img-result">
                                    <img class="cropped" src="" alt="">
                                </div>
                            </div>
                            <button class="btn save d-none">Save</button>
                            <a href="" class="btn download d-none">Download</a>
                        <% } else { %>
                            <input type="text" class="form-control" id="<%= column.Field %>" name="<%= column.Field %>">
                        <% } %>
                    <% } %>
                </div>
            <% } %>
        <% }) %>

        <button type="submit" class="btn btn-primary">Submit</button>

        <a href="/adminDashboard" class="btn">Cancel</a>
    </form>
</div>

<%- include('../partials/script', { user: 'admin' }) %>